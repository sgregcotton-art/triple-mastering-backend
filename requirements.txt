Maybe: Sierra Gregory:
	main.py
requirements.txt
runtime.txt
uploads/
mastered/
metadata/

Sierra Gregory:
	fastapi
uvicorn
numpy
soundfile
pyloudnorm
mutagen
python-multipart
fastapi==0.111.0
uvicorn==0.23.2
numpy==1.26.5
soundfile==0.12.1
pyloudnorm==0.2.1
mutagen==1.46.0
python-multipart==0.0.6
Maybe: Sierra Gregory:
	fastapi==0.111.0
uvicorn==0.23.2
numpy==1.26.5
soundfile==0.12.1
pyloudnorm==0.2.1
mutagen==1.46.0
python-multipart==0.0.6

Sierra Gregory:
		•	fastapi==0.111.0 → Latest stable FastAPI, compatible with Python 3.11.
	•	uvicorn==0.23.2 → ASGI server for FastAPI.
	•	numpy==1.26.5 → Stable version for numeric and audio calculations.
	•	soundfile==0.12.1 → Read/write WAV and FLAC.
	•	pyloudnorm==0.2.1 → LUFS loudness measurement & normalization.
	•	mutagen==1.46.0 → Embed metadata (ID3 tags, WAV metadata).
	•	python-multipart==0.0.6 → Handles file uploads in FastAPI.
	•	fastapi==0.111.0 → Latest stable FastAPI, compatible with Python 3.11.
	•	uvicorn==0.23.2 → ASGI server for FastAPI.
	•	numpy==1.26.5 → Stable version for numeric and audio calculations.
	•	soundfile==0.12.1 → Read/write WAV and FLAC.
	•	pyloudnorm==0.2.1 → LUFS loudness measurement & normalization.
	•	mutagen==1.46.0 → Embed metadata (ID3 tags, WAV metadata).
	•	python-multipart==0.0.6 → Handles file uploads in FastAPI.
